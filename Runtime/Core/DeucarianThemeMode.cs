namespace Deucarian.Theming
{
    /// <summary>
    /// Explicit color mode used to select a variant from a <see cref="DeucarianThemeFamily"/>.
    /// </summary>
    public enum DeucarianThemeMode
    {
        Light = 0,
        Dark = 1
    }
}
