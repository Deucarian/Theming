namespace Deucarian.Theming
{
    /// <summary>Explicit product experience used to resolve semantic audio.</summary>
    public enum DeucarianAudioExperience
    {
        Default = 0,
        XR = 1,
        WebGL = 2,
        Desktop = 3,
        Mobile = 4
    }
}
